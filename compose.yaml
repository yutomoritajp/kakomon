services:
  frontend:
    platform: linux/amd64
    build: ./frontend
    ports:
      - 5173:5173
    volumes:
      - ./frontend:/app
      - react_node_modules:/app/node_modules

volumes:
  react_node_modules: