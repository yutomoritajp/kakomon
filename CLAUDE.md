# 概要

このプロジェクトはIPAが主催する「データベーススペシャリスト試験」の学習のため、広く知られている「データベーススペシャリスト過去問道場」よりも学習しやすいwebアプリケーションを作成することを目的として作成する。作成者本人が個人学習で使用することを想定しており、本アプリケーションの作成を通してSPAアプリケーションの実装を学びつつ、データベーススペシャリストの合格を目指す。

## 前提

このプロジェクトはClaudeCodeを使用して、AIエージェントと共同で作業することを前提とする。
このプロジェクト内に「設計書」「ソースコード」の両方が含まれる。ユーザー主導で設計書やソースコードを書き、AIエージェントがレビューや提案などの補佐をすることを想定する。
アジャイルに近い開発手法をとるため、設計書はユーザーとAIエージェントとの認識齟齬を防ぐために作成するものとし、設計書の作成は必要最低限とする。


### 用語集

このプロジェクト全体に係る用語。

- 開発者：このプロジェクトを企画・作成する一人の人物。
- AIエージェント：このプロジェクトの補佐をするAI。基本的にはClaudeを想定する。
- ユーザー：このアプリケーションを使用する人。ユーザーが開発者自身を指すこともある。

### プロジェクト構成

```
.
├── CLAUDE.md
├── api/              ← API機能を提供するソースフォルダ
├── batch/            ← Batch機能を提供するソースフォルダ
├── db/               ← DB情報などを格納するフォルダ
├── docs/             ← 要件定義書・基本設計書などを格納するフォルダ
    └── v1/           ← サイクル・スコープごとにフォルダを作成する
└── frontend/         ← Front機能を提供するソースフォルダ
```

各フォルダ固有のルール（フォルダ構成・レビュー方針など）はフォルダごとに作成するものとし、`CLAUDE.md`には記載しない。

## CLAUDE.mdの位置付け

このファイルは要求定義書を兼ねる。
不変の要求（確実に達成したいこと）と方針を記載するが、具体的な要件はここには記載しない。（スコープ管理と柔軟さを確保するため）

より具体的かつ状況によって可変な要件については各サイクルごとに要件定義書を`docs/`フォルダに作成する。

### 進め方とAIエージェントが留意すること

本プロジェクトはアジャイルに近い反復開発で進める。各サイクルは細かく区切り、以下の1~3を繰り返す。

1. 直近で必要な機能の要件を小さなスコープで定義する。
2. 実装・およびテストをする。
3. 実際に使用して、必要な機能や不足しているものを見つける。

したがって、エージェントは以下のことに留意する。

- 設計書は「本当に必要なこと」のみが書かれているかを考える。
  - 要件定義書・設計書は開発者とAIエージェントの認識齟齬を生まないためにある。（認識齟齬を生まない実装の場合は設計書は作らない）
  - 設計書を書くために必要なことを要件定義書に書き、実装に必要なことを設計書に書く。
  - 設計途中で要件定義書を編集したり、実装途中で設計書を編集することは許容する。（ウォーターフォールではないため）
- 要件定義にない機能を設計書に書かない（必要そうでも次サイクルへ）
- 設計にない機能を実装に反映させない（ただし拡張性は意識）

## このプロジェクトに期待すること

1. 完成したアプリを使うことで、データベーススペシャリスト学習を捗らせたい。
2. PDCAサイクルを回しながらアプリをより良くしていきたい。
3. AIエージェントと共同でアプリケーションを作っていく経験を積みたい。
4. Front機能を作成することで、Reactについて学びを深めたい。
5. API機能・Batch機能を作成することで、Python, FastAPIについて学びを深めたい。
6. DBを使ったアプリケーションを作成することでPostgresqlについて学びを深めたい。また、データベース設計等を学ぶ題材にしたい。

### 「このプロジェクトに期待すること」を達成するために必要なこと

- 最初から完璧を目指さず、動くものを作って改善する取り組みをする。
- 早くアプリを使用できる状態にする。
- AIエージェントだけが理解している状態や開発者だけが理解している状態をなくす。開発者の理解が追いつかないままAIエージェントがソースコードを作ったり、開発者がAIエージェントに見えない場所に設計書を作ったりしない。
- 開発で得た知見を外部ブログサービス等にアウトプットする。

## 要求(Must)

確実に達成したい要求は以下に記載する。サイクルごとの要求は`./docs`配下の要件定義に都度記載していく。

### 1. AI質問機能の実装

「データベーススペシャリスト過去問道場」との差別化のため、AIに問題や解説についての質問ができる機能をつける。

### 2. 必要技術スタックの使用

以下の技術は確実に使用するものとする。

- React(javascript)
- Python
- FastAPI
- Postgresql
- Docker